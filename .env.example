# Content Buddy — root (Vite frontend). Copy to `.env` for local dev.
# https://vitejs.dev/guide/env-and-mode.html

# Backend origin (no path prefix — Express mounts `/auth`, `/videos`, etc. at root)
VITE_API_BASE_URL=http://localhost:3001
